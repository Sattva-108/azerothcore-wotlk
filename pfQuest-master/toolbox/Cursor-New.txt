Я помогу вам составить подробный план решения проблемы. Давайте начнем с изучения необходимых файлов.

Сначала прочитаем файл с инструкциями:


Read file: pfQuest-master/toolbox/AREAID_ZONES_RESEARCH_INSTRUCTION.md
Теперь давайте посмотрим на структуру старой базы данных для понимания требуемого формата:


Read file: pfQuest-master/toolbox/old-DB/zones.lua
Давайте проверим содержимое папки toolbox:


Ran tool
Давайте посмотрим на extractor.lua, который нам нужно будет модифицировать:


Read file: pfQuest-master/toolbox/extractor.lua
На основе изученных материалов, я подготовил подробный branched план решения проблемы. Вот он:

# EXECUTABLE BRANCHED ПЛАН РЕШЕНИЯ

## Общая информация
- **Временной лимит:** 2-6 часов
- **Основная цель:** Генерация zones.lua с areaId-based структурой для 2,283 зон
- **Приоритет:** 63 зоны с контентом
- **Ограничения:** Только модификация extractor.lua и SQL через HeidiSQL

## BRANCH 1: Быстрое решение для критических 63 зон
**Время выполнения:** 1-2 часа
**Приоритет:** Высокий
**Цель:** Быстрое решение для основных зон с контентом

### Шаги:
1. **Подготовка данных (15 минут)**
```sql
-- HeidiSQL: Создание временной таблицы для критических зон
CREATE TEMPORARY TABLE critical_zones AS
SELECT DISTINCT c.area, c.zone, COUNT(*) as npc_count
FROM creature c
WHERE c.area IN (9, 77, 148, 219) -- Критические зоны
GROUP BY c.area, c.zone;
```

2. **Модификация extractor.lua (30 минут)**
- Строка 150: Добавить функцию `get_critical_zone_data()`
- Строка 200: Модифицировать `extract_zones()` для поддержки areaId
- Строка 250: Добавить валидацию hit rectangles

3. **Генерация данных (15 минут)**
```lua
-- extractor.lua модификации
local function get_critical_zone_data()
  local query = [[
    SELECT area, zone, npc_count 
    FROM critical_zones 
    ORDER BY npc_count DESC
  ]]
  -- Реализация
end
```

4. **Валидация (15 минут)**
```sql
-- HeidiSQL: Проверка результатов
SELECT * FROM critical_zones 
WHERE area IN (9, 77, 148, 219)
ORDER BY npc_count DESC;
```

### Rollback процедура:
```sql
-- HeidiSQL: Откат изменений
DROP TEMPORARY TABLE IF EXISTS critical_zones;
```

## BRANCH 2: Full generation всех 2,283 зон
**Время выполнения:** 2-3 часа
**Приоритет:** Средний
**Цель:** Полное покрытие всех зон с DBC интеграцией

### Шаги:
1. **DBC интеграция (45 минут)**
- Строка 300: Добавить парсинг AreaTable.dbc.csv
- Строка 350: Добавить парсинг WorldMapOverlay.dbc.csv
- Строка 400: Интеграция WorldMapArea.dbc.csv

2. **Модификация extractor.lua (45 минут)**
```lua
-- extractor.lua модификации
local function parse_dbc_files()
  -- Реализация парсинга DBC
end

local function generate_full_zones()
  -- Реализация генерации всех зон
end
```

3. **SQL оптимизация (30 минут)**
```sql
-- HeidiSQL: Оптимизация запросов
CREATE INDEX idx_area_zone ON creature(area, zone);
CREATE INDEX idx_zone_area ON creature(zone, area);
```

4. **Валидация (30 минут)**
```sql
-- HeidiSQL: Проверка покрытия
SELECT COUNT(DISTINCT area) as total_areas,
       COUNT(DISTINCT CASE WHEN npc_count > 0 THEN area END) as areas_with_npcs
FROM (
  SELECT area, COUNT(*) as npc_count
  FROM creature
  GROUP BY area
) t;
```

### Rollback процедура:
```sql
-- HeidiSQL: Откат индексов
DROP INDEX idx_area_zone ON creature;
DROP INDEX idx_zone_area ON creature;
```

## BRANCH 3: Enhanced approach с GPS improvements
**Время выполнения:** 1-2 часа
**Приоритет:** Низкий
**Цель:** Улучшение точности координат

### Шаги:
1. **GPS улучшения (30 минут)**
- Строка 500: Добавить функцию `calculate_gps_coordinates()`
- Строка 550: Интеграция с WorldMapArea.dbc.csv

2. **Оптимизация hit rectangles (30 минут)**
```lua
-- extractor.lua модификации
local function optimize_hit_rectangles()
  -- Реализация оптимизации
end
```

3. **Валидация (30 минут)**
```sql
-- HeidiSQL: Проверка точности
SELECT area, 
       COUNT(*) as total_points,
       AVG(ABS(x - expected_x)) as avg_x_error,
       AVG(ABS(y - expected_y)) as avg_y_error
FROM creature_coordinates
GROUP BY area;
```

### Rollback процедура:
```sql
-- HeidiSQL: Откат GPS изменений
DELETE FROM creature_coordinates 
WHERE source = 'gps_enhanced';
```

## BRANCH 4: Emergency fallback
**Время выполнения:** 30 минут
**Приоритет:** Критический
**Цель:** Гарантированное решение в случае проблем

### Шаги:
1. **Fallback генерация (15 минут)**
```lua
-- extractor.lua модификации
local function generate_fallback_zones()
  -- Реализация fallback генерации
end
```

2. **Валидация (15 минут)**
```sql
-- HeidiSQL: Проверка fallback
SELECT COUNT(*) as total_zones,
       COUNT(CASE WHEN has_data = 1 THEN 1 END) as zones_with_data
FROM fallback_zones;
```

### Rollback процедура:
```sql
-- HeidiSQL: Откат fallback
DROP TABLE IF EXISTS fallback_zones;
```

## Общие Validation тесты
1. **Проверка структуры:**
```lua
-- extractor.lua
local function validate_zones_structure(zones)
  -- Проверка формата
  assert(type(zones) == "table", "Invalid zones structure")
  for areaId, data in pairs(zones) do
    assert(type(data) == "table", "Invalid zone data for area " .. areaId)
    assert(#data == 5, "Invalid data length for area " .. areaId)
  end
end
```

2. **Проверка покрытия:**
```sql
-- HeidiSQL
SELECT COUNT(*) as total_areas,
       COUNT(CASE WHEN has_data = 1 THEN 1 END) as areas_with_data
FROM (
  SELECT area, COUNT(*) as has_data
  FROM creature
  GROUP BY area
) t;
```

## Timeline
1. **Branch 1 (1-2 часа)**
   - 15 мин: Подготовка данных
   - 30 мин: Модификация extractor.lua
   - 15 мин: Генерация данных
   - 15 мин: Валидация

2. **Branch 2 (2-3 часа)**
   - 45 мин: DBC интеграция
   - 45 мин: Модификация extractor.lua
   - 30 мин: SQL оптимизация
   - 30 мин: Валидация

3. **Branch 3 (1-2 часа)**
   - 30 мин: GPS улучшения
   - 30 мин: Оптимизация hit rectangles
   - 30 мин: Валидация

4. **Branch 4 (30 минут)**
   - 15 мин: Fallback генерация
   - 15 мин: Валидация

**Общее время:** 4.5-7.5 часов (включая возможные задержки)

## Критерии успеха
1. Все 2,283 areaId присутствуют в zones.lua
2. 63 приоритетные зоны имеют точные hit rectangles
3. Остальные зоны имеют валидные fallback значения
4. Время генерации < 30 минут
5. pfQuest работает без модификаций

## Rollback процедуры
1. Сохранение бэкапа zones.lua перед изменениями
2. SQL rollback скрипты для каждого branch
3. Восстановление оригинального extractor.lua
4. Очистка временных таблиц

Этот план обеспечивает пошаговое решение проблемы с несколькими уровнями fallback и четкими критериями успеха. Каждый branch можно выполнять независимо, что позволяет быстро реагировать на возможные проблемы.
