# СТАТУС pfQuest extractor для AzerothCore WotLK

## ✅ ЧТО РАБОТАЕТ:
- **Units (существа)**: ✅ Координаты работают, 29947 существ с локализацией
- **Objects (объекты)**: ✅ Координаты работают, 1116 строк
- **Quests (квесты)**: ✅ Данные извлекаются, 2041 строка
- **Meta**: ✅ Флайтмастеры работают (175 записей)
- **Локализация**: ✅ Имена существ на всех языках

## ❌ ОСНОВНЫЕ ПРОБЛЕМЫ:

### 1. **init.lua** - СЛОМАНА функция serialize()
```
Текущий результат: pfDB = table: 00BF52E0
Должно быть: полная структура pfDB с данными
```

### 2. **zones-wotlk.lua** - ПУСТАЯ
```
Текущий результат: pfDB["zones"]["data-wotlk"] = {}
Проблема: DBC таблицы AreaTable не загружаются правильно
```

### 3. **items-wotlk.lua** - ПУСТЫЕ ЗАПИСИ
```
Текущий результат: [100] = {}, [10000] = {}, ...
Проблема: Данные предметов не заполняются (дроп-шансы, локация)
```

## 🔧 ЧТО НУЖНО ИСПРАВИТЬ:

### ПРИОРИТЕТ 1: Исправить serialize() функцию
- Проблема в функции serialize() - она выводит ссылку на таблицу вместо содержимого
- Найти и исправить serialize() в extractor.lua

### ПРИОРИТЕТ 2: Исправить зоны (zones)
- Проверить загрузку DBC таблицы AreaTable_wotlk
- Warning: "Failed to query zones from DBC tables"

### ПРИОРИТЕТ 3: Заполнить данные предметов (items)
- Сейчас создаются пустые записи [id] = {}
- Нужно добавить: дропается с кого, шанс дропа, где найти

## 📂 БЫСТРЫЙ РЕЖИМ ДЛЯ РАЗРАБОТКИ:
Добавить в начало extractor.lua:
```lua
local FAST_MODE = true  -- true = 100 существ, false = все 29947
```

## 🎯 СЛЕДУЮЩИЕ ШАГИ:
1. Исправить serialize() функцию для init.lua
2. Исправить загрузку зон из DBC
3. Заполнить данные предметов
4. Протестировать с FAST_MODE = true

## 📍 КОМАНДЫ:
```bash
cd C:\Users\user\Desktop\azerothcore-wotlk\pfQuest-master\toolbox
lua extractor.lua
```

**СТАТУС: Частично работает, нужны исправления в 3 ключевых областях**
